# ── Database ─────────────────────────────────────────────────────────────────
# Managed Postgres connection string. docker-compose passes this straight
# through to python-api — there is no database container in the stack.
# Supabase: Project Settings → Database → Connection string → URI
DATABASE_URL=postgresql://user:password@db.xxxxxxxx.supabase.co:5432/postgres

# ── AI Backend ───────────────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here

# ── Admin Dashboard ──────────────────────────────────────────────────────────
ADMIN_PASSWORD=change_me_secure_admin_password

# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=change_me_to_64_char_hex_string

# Protects POST /send-message
SEND_MESSAGE_API_KEY=change_me

# ── WhatsApp gateway auth ────────────────────────────────────────────────────
# Shared secret for the gateway's /send, /qr and /reconnect endpoints.
# docker-compose passes this to BOTH python-api and whatsapp-server, so one
# value here covers both. The gateway runs with NODE_ENV=production in compose
# and will refuse those endpoints (503) until this is set.
# Generate one: openssl rand -hex 32
GATEWAY_TOKEN=change_me_to_64_char_hex_string

# ── CORS ─────────────────────────────────────────────────────────────────────
# In production, replace with your VM's IP or custom domain (no trailing slash)
# e.g. ALLOWED_ORIGINS=http://34.123.45.67  or  https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

